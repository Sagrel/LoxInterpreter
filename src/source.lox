
function factorial(a)
{
    if a < 2 return 1;
    return a * factorial(a - 1);
};

function fib(a)
{
    if a == 0 return 0;
    if a < 3 return 1;
    return fib(a - 1) + fib(a - 2);
};

res = factorial(5);
res = fib(10);